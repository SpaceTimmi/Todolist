import Logo from '../assests/home.png'

const Header = () => {
    return (
        <div className="pg-header">
            <img src={Logo} alt="logo"/>
            Header
        </div>
    )
}

export default Header;